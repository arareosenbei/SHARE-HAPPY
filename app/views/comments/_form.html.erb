<%= form_with(model:[post, comment], data: {remote: true} ) do |f| %>
  <%= f.text_area :body, rows:'5', class: "",placeholder: "コメントを入力" %>
  <%= f.submit "コメント", class: "btn btn-outline-primary p-submit" %>
<% end %>